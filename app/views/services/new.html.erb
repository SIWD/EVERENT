<% content_for :title, 'Neue Dienstleistung' %>

<%= render 'form' %>

<% if current_user %>
    <% if User.find(current_user).profile %>
        <%= link_to 'Zurück zum Profil', profile_path(User.find(current_user).profile) %>
    <% end %>
<% end %>