
<table>
  <colgroup>
    <col width="130">
    <col width="190">
    <col width="420">
    <col width="150">
  </colgroup>
  <thead>
  <tr>
    <th>Name</th>
    <th>Teaser</th>
    <th>Description</th>
    <th>Business</th>
    <!--<th colspan="3"></th>-->
  </tr>
  </thead>

  <tbody>
  <tr><td>&nbsp;</td></tr>


  <% @services.each do |service| %>
      <tr>
        <td><%= service.name %></td>
        <td><%= service.teaser %></td>
        <td><%= service.description %></td>
        <td><%= service.business.name %></td>
        <td><%= link_to ' Anzeigen ', service %></td>


        <% if @userServices %>
            <% if ( @userServices.include?(service) )  %>
                <td>&nbsp;|&nbsp;<%= link_to 'Bearbeiten', edit_service_path(service) %></td>
            <% end %>
        <% end %>


        <% if @userServices %>
            <% if ( @userServices.include?(service) ) %>
                <td>&nbsp;|&nbsp;<%= link_to 'Löschen', service, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            <% end %>
        <% end %>

      </tr>

  <tr><td>&nbsp;</td></tr>
  <% end %>
  </tbody>
</table>

