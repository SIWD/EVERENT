<%= form_for(@branch) do |f| %>
  <% if @branch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@branch.errors.count, "error") %> prohibited this branch from being saved:</h2>

      <ul>
      <% @branch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, :required => true %>
  </div>
    <div class="field">
      <% BranchCategory.all.each do |bc| %>
          <%= f.radio_button :branchCategory_id, bc.id, :required => true %>
          <%= f.label "branchCategory_id_#{bc.id}", bc.name %><br>
      <% end %>
    </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
