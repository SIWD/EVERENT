<%= form_for(@agb) do |f| %>
  <% if @agb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agb.errors.count, "error") %> prohibited this agb from being saved:</h2>

      <ul>
      <% @agb.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
