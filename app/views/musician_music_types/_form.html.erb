<%= form_for(@musician_music_type) do |f| %>
  <% if @musician_music_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@musician_music_type.errors.count, "error") %> prohibited this musician_music_type from being saved:</h2>

      <ul>
      <% @musician_music_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
