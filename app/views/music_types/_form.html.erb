<%= form_for(@music_type) do |f| %>
  <% if @music_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@music_type.errors.count, "error") %> prohibited this music_type from being saved:</h2>

      <ul>
      <% @music_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
